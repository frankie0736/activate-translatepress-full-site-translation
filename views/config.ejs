<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>配置 - TranslatePress 激活工具</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      padding-top: 2rem;
      background-color: #f8f9fa;
    }
    .container {
      max-width: 800px;
    }
    .header {
      margin-bottom: 2rem;
      text-align: center;
    }
    .config-container {
      background-color: white;
      padding: 2rem;
      border-radius: 10px;
      box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.1);
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>配置设置</h1>
      <p class="lead">调整防反爬虫策略和访问参数</p>
    </div>

    <div class="config-container">
      <form action="/update-config" method="POST">
        <div class="mb-3">
          <label for="delayMin" class="form-label">最小页面间隔时间 (毫秒)</label>
          <input 
            type="number" 
            class="form-control" 
            id="delayMin" 
            name="delayMin" 
            value="<%= options.delayMin %>"
            min="1000"
          >
          <div class="form-text">页面间最小延迟时间，建议至少3000毫秒</div>
        </div>

        <div class="mb-3">
          <label for="delayMax" class="form-label">最大页面间隔时间 (毫秒)</label>
          <input 
            type="number" 
            class="form-control" 
            id="delayMax" 
            name="delayMax" 
            value="<%= options.delayMax %>"
            min="3000"
          >
          <div class="form-text">页面间最大延迟时间，建议至少10000毫秒</div>
        </div>

        <div class="mb-3">
          <label for="loadWaitTime" class="form-label">页面加载等待时间 (毫秒)</label>
          <input 
            type="number" 
            class="form-control" 
            id="loadWaitTime" 
            name="loadWaitTime" 
            value="<%= options.loadWaitTime %>"
            min="1000"
          >
          <div class="form-text">页面加载后等待时间，确保TranslatePress有足够时间初始化</div>
        </div>

        <div class="mb-3">
          <label class="form-label">模拟人类行为</label>
          <div class="form-check">
            <input 
              class="form-check-input" 
              type="radio" 
              name="simulateHuman" 
              id="simulateHumanYes" 
              value="true"
              <%= options.simulateHuman ? 'checked' : '' %>
            >
            <label class="form-check-label" for="simulateHumanYes">
              是
            </label>
          </div>
          <div class="form-check">
            <input 
              class="form-check-input" 
              type="radio" 
              name="simulateHuman" 
              id="simulateHumanNo" 
              value="false"
              <%= !options.simulateHuman ? 'checked' : '' %>
            >
            <label class="form-check-label" for="simulateHumanNo">
              否
            </label>
          </div>
          <div class="form-text">自动滚动和随机停顿，模拟真实用户</div>
        </div>

        <div class="mb-3">
          <label for="maxConcurrent" class="form-label">最大并发数</label>
          <input 
            type="number" 
            class="form-control" 
            id="maxConcurrent" 
            name="maxConcurrent" 
            value="<%= options.maxConcurrent %>"
            min="1"
            max="5"
          >
          <div class="form-text">同时处理的URL数量，建议保持为1以避免被检测</div>
        </div>

        <div class="mb-3">
          <label for="userAgent" class="form-label">User Agent</label>
          <input 
            type="text" 
            class="form-control" 
            id="userAgent" 
            name="userAgent" 
            value="<%= options.userAgent %>"
          >
          <div class="form-text">浏览器身份标识</div>
        </div>

        <div class="d-flex justify-content-between">
          <a href="/" class="btn btn-secondary">返回首页</a>
          <button type="submit" class="btn btn-primary">保存配置</button>
        </div>
      </form>
    </div>

    <footer class="mt-4 text-center text-muted">
      <p>TranslatePress激活工具 &copy; <%= new Date().getFullYear() %></p>
    </footer>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 