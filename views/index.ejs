<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TranslatePress 激活工具</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      padding-top: 2rem;
      background-color: #f8f9fa;
    }
    .container {
      max-width: 800px;
    }
    .header {
      margin-bottom: 2rem;
      text-align: center;
    }
    .form-container {
      background-color: white;
      padding: 2rem;
      border-radius: 10px;
      box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.1);
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>TranslatePress 激活工具</h1>
      <p class="lead">输入网站的Sitemap URL，系统将自动访问所有页面以激活TranslatePress翻译</p>
    </div>

    <div class="form-container">
      <% if (taskState.isRunning) { %>
        <div class="alert alert-info">
          <p>任务正在执行中，请查看 <a href="/progress" class="alert-link">进度页面</a></p>
        </div>
      <% } else if (taskState.error) { %>
        <div class="alert alert-danger">
          <p>执行出错: <%= taskState.error %></p>
        </div>
      <% } %>

      <form action="/process-sitemap" method="POST">
        <div class="mb-3">
          <label for="sitemapUrl" class="form-label">Sitemap URL</label>
          <input 
            type="url" 
            class="form-control" 
            id="sitemapUrl" 
            name="sitemapUrl" 
            placeholder="例如: https://example.com/sitemap.xml 或 https://example.com/sitemap_index.xml"
            required
          >
          <div class="form-text">输入网站的Sitemap XML文件的URL，支持sitemap_index.xml</div>
        </div>
        <button type="submit" class="btn btn-primary" <%= taskState.isRunning ? 'disabled' : '' %>>
          开始处理
        </button>
      </form>
    </div>

    <footer class="mt-4 text-center text-muted">
      <p>TranslatePress激活工具 &copy; <%= new Date().getFullYear() %></p>
    </footer>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 